import React from "react";
import UserProfile from "../../../components/UserProfile";
const page = async () => {
  return (
    <div className=" w-screen">
      <UserProfile />
    </div>
  );
};

export default page;
